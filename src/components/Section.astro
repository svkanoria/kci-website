---
export interface Props {
  isBox?: boolean;
  isCompact?: boolean;
  class?: string;
  bgImage?: string;
}

const { isBox, isCompact, class: className, bgImage } = Astro.props;
---

<section
  class:list={[
    {
      ["p-4 md:p-8"]: isBox,
      ["pb-4 md:pb-8 last:pb-0"]: !isBox && isCompact,
      ["pb-4 md:pb-12 last:pb-0"]: !isBox && !isCompact,
    },
    className,
  ]}
  style={{ backgroundImage: bgImage }}
>
  <slot />
</section>
